ORI-C - REPARATION ET INTEGRATION 7 AXES, LIVRAISON SURE

Le commit accidentel : f5fc7447277abc25eee04662cdb536a626c449e2
Le parent sain       : 6993cfd6d0e76df9541834ffe7113a125bdac160

1. Pour réparer le dépôt actuellement cassé :
   python REPARER_DEPOT_APRES_IMPORT_ACCIDENTEL.py --push

   Le script refuse d'agir si HEAD n'est pas exactement le commit accidentel.
   Il exécute git revert, vérifie que l'arbre obtenu est exactement celui du parent sain, hydrate LFS si disponible, puis exécute verifier_dossier.py avant tout push.

2. Après réparation, pour intégrer uniquement le travail 7 axes :
   python APPLIQUER_7_AXES_SAFE.py CHEMIN_DU_DEPOT_ORI-C

   Ce script ne peut écrire que sous :
   plan_directeur/exploitation_donnees_existantes_2026_08_16/
   Il bloque les noms sensibles de la racine et reconstruit MANIFEST.sha256 avec build_manifest.py du dépôt.

3. archive_scientifique_autonome/ conserve les données et reproductions récupérées. Son manifeste est nommé ARCHIVE_PACKAGE_MANIFEST.sha256. Aucun fichier nommé MANIFEST.sha256 n'existe dans cette nouvelle livraison.

IMPORTANT : les gros bruts externes ne sont pas injectés dans le dépôt par le payload. Leurs SHA-256 et leur provenance sont conservés dans PROVENANCE_ENTREES.json.
