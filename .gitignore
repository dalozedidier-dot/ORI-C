__pycache__/
*.py[cod]
.pytest_cache/
.pytest-tmp/
build/
dist/
*.egg-info/
.venv/
venv/

# Les données brutes volumineuses seront archivées sur OSF/Zenodo.
plateforme/campagne_maximale_reelle/sources_brutes/*
!plateforme/campagne_maximale_reelle/sources_brutes/README.md


# Instantane redondant du depot, commite par erreur le 7 aout 2026.
# Ses 1320 entrees etaient toutes deja suivies ; il pesait 51 Mo et faisait
# echouer `verifier_dossier.py`, qui ne consulte pas .gitignore.
/.github.zip

# Instantanes du depot deposes a la racine, commites par erreur. Ils ne
# contiennent que des fichiers deja suivis, pesent des dizaines de megaoctets
# et font echouer `verifier_dossier.py`, qui ne consulte pas .gitignore.
# Aucune archive legitime ne vit a la racine : celles des donnees tierces sont
# sous donnees_externes/ et suivies par Git LFS.
/*.zip
/.claude/
/01_branche_matiere/memoire_materielle_reelle/derive/execution/
